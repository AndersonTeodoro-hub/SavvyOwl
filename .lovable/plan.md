

## Usar logo oficial como favicon

O ficheiro `public/favicon.svg` actual é diferente da logo oficial que enviaste. A solução é simples:

### Alteração

1. **Copiar** `user-uploads://savvyowl-logo-transparent.svg` para `public/favicon.svg` (substituir o existente)

O `index.html` já aponta para `/favicon.svg` (linha 11), portanto não precisa de mais nenhuma alteração.

